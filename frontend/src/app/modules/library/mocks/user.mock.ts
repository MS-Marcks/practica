import { User } from '../../../shared/interfaces/user.interface';

export const USER: User = {
  access_token: "<TOKEN>",
  tokenType: "Bearer ",
  user: {
    userId: "ID",
    username: "EXAMPLE"
  }
}
